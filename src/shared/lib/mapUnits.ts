/**
 * Number of map units per placed tile.
 * Each tile occupies a 6×6 unit cell in the map coordinate system.
 * Used for serialization, deserialization, and rendering alignment.
 */
export const MAP_UNITS_PER_TILE = 6;
